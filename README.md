# Footballistika Bot

Телеграм-бот для збору футбольних прогнозів з підрахунком балів і простим текстовим сховищем.

## Вимоги

- Python 3.9+
- Bot Token, отриманий у [@BotFather](https://t.me/BotFather)
- Список Telegram ID адміністраторів (через кому)

## Швидкий старт

```bash
cd footballistika
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Створи файл `.env` у корені проєкту:

```
TELEGRAM_BOT_TOKEN=1234567890:ABCDEF
ADMIN_IDS=11111111,22222222
```

Запуск:

```bash
python bot.py
```

## Структура файлів сховища

Усі дані лежать у `data/` та зберігаються у форматі “рядок = значення, розділені `|`”.

1. `matches.txt` — майбутні події:  
   `match_id|team1|team2|score1|score2|status`
2. `predictions.txt` — прогнози користувачів:  
   `match_id|user_id|username|pred_score1|pred_score2|timestamp|points_awarded`  
   `timestamp` — час подачі прогнозу, використовуємо як “дату прогнозу”.
3. `leaderboard.txt` — сума балів:  
   `user_id|username|points`
4. `prediction_result_accuracy.txt` — частка правильних прогнозів результату:  
   `user_id|username|predictions|result_accuracy_percent`
5. `prediction_goal_accuracy.txt` — точність рахунків по голах:  
   `user_id|username|predictions|goal_accuracy_percent`

Файли заповнюються автоматично через бот.
`result_accuracy_percent` показує частку правильних прогнозів результату (перемога/нічия/поразка),
а `goal_accuracy_percent` — середню точність по забитих голах, де для кожної команди береться відношення ближчого значення до більшого з фактичного та передбаченого рахунку й результати усереднюються.
Обидві статистики з’являються лише після завершення бодай одного матчу.

## Функціонал користувача

- **Зробити прогноз** — бот показує найближчий матч без прогнозу користувача та просить рахунок `X:Y`. Після збереження запропонує наступний матч або повідомить, що все виконано.
- **Таблиця** — виводить топ-10 по балах і позицію поточного користувача, якщо він поза топом.
- Після першого звернення до бота користувач автоматично з'являється у таблиці з 0 балів.

## Адмін-панель

- На головній клавіатурі адміністратор бачить кнопку **Адмінка**.
- Усередині відкривається меню:
  - `➕ Додати матч` — бот послідовно просить назви команд, показує кнопки для підтвердження/повернення та робить фінальне підтвердження перед створенням.
  - `📋 Ввести результат` — бот показує наступний матч зі статусом pending і просить фінальний рахунок `X:Y`.
  - `📊 Середній прогноз` — показує середнє арифметичне забитих голів по всіх прогнозах на кожен актуальний матч.
  - `📜 Всі прогнози` — виводить згрупований список прогнозів користувачів на кожен матч.
  - `🎯 Влучність результату` — таблиця по відсотку правильних перемог/нічиї/поразок для кожного прогнозиста.
  - `🥅 Точність по голах` — таблиця точності по рахунках (відсоток точності забитих голів кожної команди).
  - `⬅️ Назад` — повернення до звичайних кнопок.
- Після введення результату бали автоматично розраховуються, а таблиця оновлюється.

## Нарахування балів

- Точний рахунок → **5** балів.
- Вірний тип результату (перемога конкретної команди або нічия) → **1** бал.
- Якщо прогноз хибний — 0.

## Корисні нотатки

- Адмінські команди можна розширити або обмежити, відредагувавши `ADMIN_IDS` у `.env`.
- Зберігання у текстових файлах полегшує ручне редагування, але за потреби легко перейти на БД.
- Для інших середовищ можна переозначити шлях до даних змінною `DATA_DIR`.
